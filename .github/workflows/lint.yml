name: Lint
on:
  push:
    branches: [main]
  pull_request:
  # Dispatched by release.yml for the changeset-release/main PR, whose
  # pull_request runs are parked unexecuted behind an approval gate.
  workflow_dispatch:
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  oxlint:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      format: ${{ steps.format.outcome }}
      fallow: ${{ steps.fallow.outcome }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/setup@84cb39b217b10273981911c288cd62326dc7c6d2 # v2.0.2
        with:
          runtime: node@24
          cache: true
      - name: Restore Turbo task cache
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: .turbo/cache
          key: turbo-${{ runner.os }}-${{ runner.arch }}-node24-lint-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref_name }}-${{ github.sha }}
          restore-keys: |
            turbo-${{ runner.os }}-${{ runner.arch }}-node24-lint-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref_name }}-
            turbo-${{ runner.os }}-${{ runner.arch }}-node24-lint-push-main-
      - run: pnpm build --filter="./packages/*"
      - name: Check formatting
        id: format
        continue-on-error: true
        run: pnpm format:check
      - name: Check dead code
        id: fallow
        continue-on-error: true
        run: pnpm fallow:dead
      - run: pnpm oxlint --format=github .
  oxfmt:
    needs: oxlint
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Report formatting result
        env:
          RESULT: ${{ needs.oxlint.outputs.format }}
        run: test "$RESULT" = success
  dead-code:
    needs: oxlint
    if: ${{ always() }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Report dead-code result
        env:
          RESULT: ${{ needs.oxlint.outputs.fallow }}
        run: test "$RESULT" = success
