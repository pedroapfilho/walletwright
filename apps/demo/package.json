{
  "name": "demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "clean": "rm -rf dist node_modules .walletwright test-results",
    "lint": "oxlint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:cache": "node scripts/build-cache.ts",
    "test:cache:one": "node scripts/cache-one.ts",
    "test": "playwright test"
  },
  "dependencies": {
    "@wallet-standard/app": "^1.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "walletwright": "workspace:*"
  }
}
